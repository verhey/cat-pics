[build]
	command = "# no build command"
	functions = "functions"
	publish = "public"
	image = "ubuntu:noble"

[build.environment]
	NODE_VERSION = "22"

[functions]
    node_bundler = "esbuild"
    included_files = ["functions/cats/*.jpg"]
